{% extends 'base.html.twig' %}

{% block title %}Designation{% endblock %}

{% block body %}
    <h1>Designation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ designation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ designation.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('designation_index') }}">back to list</a>

    <a href="{{ path('designation_edit', {'id': designation.id}) }}">edit</a>

    {{ include('designation/_delete_form.html.twig') }}
{% endblock %}
